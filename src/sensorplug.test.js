/**
 * Created by elimenchaca on 11/7/17.
 */
var expect = require('chai').expect;
var sensorplug = require('./sensorplug');

describe('sensorplug', function () {
    describe('detectOrientationSensor', function () {
        it('should detect device orientation', function () {
            expect(true).to.be.true;
        });
    });
    describe('handleOrientationSensor', function () {
        it('should handle device orientation event', function () {
            expect(true).to.be.true;
        });
    });
});